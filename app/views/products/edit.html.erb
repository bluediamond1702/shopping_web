<% provide(:title,       'Edit product') %>
<% provide(:button_text, 'Save changes') %>
<% provide(:url,         product_path(@product))%>

<h1>Update product</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form', product: @product %>
  </div>
</div>
